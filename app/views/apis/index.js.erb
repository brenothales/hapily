$boxes = $('<%= j render(@all) %>')

$('#apis').append( $boxes ).imagesLoaded( function(){
  $('#apis').masonry( 'reload');
});
<% if @all.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@all) %>');
<% else %>
  $('.pagination').remove();
<% end %>