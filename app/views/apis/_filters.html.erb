Filter by category:
<% @categories.each do |cat| %>
<%= link_to cat["name"], apis_path(tag: cat["name"]), remote: true, :class => "filter" %> |
<% end %>
<%= link_to "All", apis_path, remote: true, :id => "all-apis" %>